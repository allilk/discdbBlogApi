import { Sequelize } from "sequelize/dist";

// const context: Sequelize = new Sequelize('app-message', 'joemama', 'password123', {
//     host: 'postgres',
//     dialect: 'postgres'
// })

// context.query('SELECT * FROM message');